# Настройки деплоя (редактируйте при смене сервера)
SERVER_IP=89.108.98.72
HTTP_PORT=80

# SSL (Let's Encrypt) — нужен домен с A-записью на SERVER_IP
# DOMAIN=orenplace.ru
# SSL_EMAIL=admin@orenplace.ru
